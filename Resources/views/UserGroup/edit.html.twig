{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% edit'|trans({'%entity%': 'UserGroup'}, 'UserGroup') }} - {{ parent() }}{% endblock %}

{% block app_menu %}
    {{ include("FlowerCoreBundle:Default:admin_nav.html.twig") }}
{% endblock %}

{% block body %}
    <section class="content-header">
        <h1><a href="{{ path('admin_dash') }}"> {{ 'Settings'|trans}}</a>
            /
            <a href={{ path('usergroup') }}>
                {{ 'UserGroup' | trans({}, 'UserGroup')}}
            </a>
            /
            {{ '%entity% edit'|trans({'%entity%': 'UserGroup'}, 'UserGroup') }}
        </h1>
    </section>    
    <section class="content">
        <div class="box">
            <div class="box-body">

            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>
                    <button class="btn btn-primary" type="submit">{{ 'Edit'|trans({}, 'UserGroup') }}</button>
                </p>
            {{ form_end(edit_form) }}
            </div>
            <div class="box-footer">
            </div>
        </div>
    </section>
{% endblock %}


